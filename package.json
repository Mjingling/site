{
  "name": "personal-site",
  "version": "0.1.0",
  "private": true,
  "description": "个人静态网站：VitePress + 片段索引 + RSS",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run snippets-index && npm run rss && vitepress dev docs",
    "build": "npm run snippets-index && npm run rss && vitepress build docs",
    "preview": "vitepress preview docs",
    "snippets-index": "node scripts/gen-snippets-index.mjs",
    "rss": "python3 scripts/gen_rss.py"
  },
  "devDependencies": {
    "medium-zoom": "^1.1.0",
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
